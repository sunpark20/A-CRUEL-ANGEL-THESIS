# VS Code 렌더링 깨짐 수정 진행상황

## 원인 추정
- M1 Mac + Electron GPU 렌더링 충돌
- UVR5 오디오 작업 시 하드웨어 가속 사용과 연관 가능성

## 수행한 작업

### 1. 하드웨어 가속 비활성화
- 파일: `~/.vscode/argv.json`
- `"disable-hardware-acceleration": true` 주석 해제하여 활성화

### 2. GPU 캐시 삭제
- `~/Library/Application Support/Code/GPUCache/` 폴더 삭제

### 3. VS Code 재시작 필요
- **Cmd+Q**로 완전 종료 후 재실행

## 상태
- [x] argv.json 수정 완료
- [x] GPU 캐시 삭제 완료
- [ ] VS Code 재시작 후 렌더링 정상 확인 (사용자 직접 확인)

## 문제 지속 시 추가 조치
- Electron 플래그 조정 (`--disable-gpu`, `--disable-gpu-compositing`)
- VS Code 설정에서 `terminal.integrated.gpuAcceleration: "off"` 적용
- VS Code 완전 재설치
